export class Circle {
    color: string;
    radius: number;

    constructor(color: string, radius: number) {
        console.log('new Cirlce');
        this.color = color;
        this.radius = radius;
    }
}